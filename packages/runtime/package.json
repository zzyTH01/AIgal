{
  "name": "@ag/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Application 层：Turn Orchestrator、游戏/叙事/记忆服务编排、Application API",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "pretest": "pnpm --filter @ag/schemas build && pnpm --filter @ag/core build && pnpm --filter @ag/world build && pnpm --filter @ag/memory build && pnpm --filter @ag/context build && pnpm --filter @ag/option build && pnpm --filter @ag/llm build && pnpm --filter @ag/narrative build && pnpm --filter @ag/persistence build && pnpm --filter @ag/st-adapter build"
  },
  "dependencies": {
    "@ag/character": "workspace:*",
    "@ag/context": "workspace:*",
    "@ag/core": "workspace:*",
    "@ag/llm": "workspace:*",
    "@ag/memory": "workspace:*",
    "@ag/narrative": "workspace:*",
    "@ag/option": "workspace:*",
    "@ag/persistence": "workspace:*",
    "@ag/schemas": "workspace:*",
    "@ag/world": "workspace:*"
  }
}
