{
  "name": "@ag/llm",
  "version": "0.1.0",
  "private": true,
  "description": "LLM Gateway：Port + Provider + TestProvider",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "pretest": "pnpm --filter @ag/schemas build"
  },
  "dependencies": {
    "@ag/schemas": "workspace:*",
    "zod": "^3.25.76"
  }
}
